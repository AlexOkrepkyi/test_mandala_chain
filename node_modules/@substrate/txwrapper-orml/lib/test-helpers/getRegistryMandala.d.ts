import { TypeRegistry } from '@polkadot/types';
/**
 * Mandala registry creator for testing.
 *
 * @param specVersion
 * @param metadataRpc
 */
export declare function getRegistryMandala(specVersion: number, metadataRpc: string): TypeRegistry;
